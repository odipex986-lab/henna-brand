FROM node:22-bookworm-slim
RUN npm install -g pnpm
WORKDIR /app
COPY . .
RUN pnpm install
WORKDIR /app/artifacts/al-mehandi
RUN BASE_PATH=/ PORT=4173 pnpm build
EXPOSE 4173
CMD ["node_modules/.bin/vite", "preview", "--host", "0.0.0.0", "--port", "4173"]
